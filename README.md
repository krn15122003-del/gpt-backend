# gpt-backend
AI backend with env-based configuration
# AI Project
## Create the .env file

This project uses a `.env` file to store secret configuration like API keys.  
Follow these steps to set it up:

### Step 1: Copy the template
Run this in the project root:

```bash
cp .env.example .env
